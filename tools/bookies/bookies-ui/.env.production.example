# Copy to .env.production before `npm run build` (Vite inlines VITE_* at build time).
# For Cloudflare “Upload assets” / wrangler deploy, the built files must already contain these.
VITE_API_URL=https://bookies.gladesystems.workers.dev
VITE_ADMIN_SECRET=bookies-admin-secret
